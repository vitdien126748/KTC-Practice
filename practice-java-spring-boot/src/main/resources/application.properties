spring.application.name=practice-java-spring-boot

# --- CẤU HÌNH DATABASE ---
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/practice_java_spring_boot?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Skipper1267489052}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- CẤU HÌNH JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update          
spring.jpa.show-sql=true                      
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true